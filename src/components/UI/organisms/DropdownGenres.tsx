import { useEffect } from "react";
import { useState } from "react";
export const DropdownGenres = (data: any, name: string) => {
  switch (name) {
    case "Жанры":
      return <>kek1</>;
      <section className="dropdown-genres">
        {/* <div>{props?.props}</div> */}
        lol
      </section>;
    default:
      return <>kek2</>;
  }
};
